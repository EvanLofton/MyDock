# Dock —— Windows 11 上的 macOS 风格程序坞

<img src="docs/images/icon.png" width="96" align="right" alt="应用图标">

![Dock 截图](docs/images/dock.png)

一个常驻桌面的 Dock：底部悬浮、毛玻璃、悬停鱼眼放大、点击启动/切换应用、
右键菜单（含以管理员身份运行）、自动隐藏、系统托盘、配置持久化。

> 上面那张是实拍（不是效果图）：Dock 浮在桌面最底层，下面是任务栏 ——
> 两者**零重叠**共存。图标都是真实的程序图标（含大文件夹）。

> **当前状态：P1 完成。** 功能项全部实现，可自动化的验证项全部通过。
> 剩余两项验收需要**会话之外的条件**（真实 8 小时流逝时间 / 人工确认 UAC 弹窗），见文末。

---

## 快速开始

**前置**：Windows 10/11 x64、[Rust](https://rustup.rs/)（stable）、[Node.js](https://nodejs.org/) 18+。
不需要装 Visual Studio —— Tauri 会把 MSVC 构建工具当依赖自动处理（若报缺 `link.exe`，装一次
「Visual Studio Build Tools + 桌面 C++ 工作负载」即可）。WebView2 运行时 Win11 自带。

```powershell
# ① 前端（生成 app/dist —— tauri.conf.json 的 frontendDist 指向它，缺了后端起不来）
cd app
npm install
npm run build

# ② 后端：开发运行（Dock 会立刻出现在屏幕底部）
cd src-tauri
cargo run
```

想要**可分发版本 / 安装包**（`bundle` 已开）：

```powershell
cd app
npm run build
cd src-tauri
cargo build --release            # → target\release\dock-app.exe，直接就能用
cargo install tauri-cli --version "^2"   # 可选：装了才有安装包
cargo tauri build                # → 生成 .msi / .exe 安装包
```

> **没有控制台窗口**是刻意的（GUI 子系统），所有输出都进日志文件 ——
> 位置和读法见[「日志与崩溃取证」](#日志与崩溃取证)。出错先看那里，不要靠猜。

- **第一次运行会自动探测本机装了哪些常用应用**并预置一屏（系统位置 + 资源管理器 +
  浏览器 + 终端 + 设置）—— 路径与名字全部现查（`App Paths` 注册表 / Shell 显示名 /
  文件版本信息），**代码里没有任何写死的安装路径或应用名**，详见
  [「换台机器能直接用吗」](#换台机器能直接用吗全是现查的)。

- **不自动隐藏**是默认值（Dock 常驻显示）。想让它贴边自动隐藏：托盘菜单或设置页里打开。
  开了之后鼠标移到屏幕**最底边**唤出，离开约 0.4 秒收起。
  实测唤出后 Dock 停在 `900..1020`、任务栏在 `1020..1080`，**零重叠**。
- **Dock 上显示哪些图标、按什么顺序，完全由你决定。** 出厂只是给了几个初值。
  运行状态**不影响位置、也不决定是否出现** —— 没在跑就只是没有白点。
  添加只有两个入口：**从桌面/资源管理器拖进来**、托盘**「添加应用…」**；移除走右键。
- **拖动 Dock 上的图标可以改顺序**，松手即保存（重启后保持）。邻居图标会让位，
  拖到哪儿就在哪儿放开。
- **把图标拖出 Dock（往上拖离面板）再松手 = 从 Dock 移除**。拖出去时面板描一圈红边，
  并在面板顶部显示文字提示「松开即移除『应用名』」——
  **拖回面板内松手则不删**，也不会改顺序。
- **从桌面或资源管理器把程序拖到 Dock 上** → 自动识别身份、去重、**追加到最右侧**；
  如果它**已经在运行**，运行指示点（白点）会立刻亮起来，不用等轮询。
  支持 `.exe`、`.lnk`（含指向商店应用的快捷方式）和 **`.url`（Internet 快捷方式）**；
  文件夹和别的文件会被拒绝并说明原因。
- **Steam / Epic 这类游戏**：在游戏库里右键 → 管理 → **添加桌面快捷方式**，
  然后把桌面上生成的 `.url`（例如 `Wallpaper.url`）**拖到 Dock 上**（或托盘「添加应用…」里选它）。
  图标取的是快捷方式 `IconFile=` 指的那个图标（Steam 指的就是游戏自己的启动器），
  名字取快捷方式的名字 —— **想改名就把桌面那个快捷方式改名**。
  点它 = `ShellExecute` 打开这个 `.url`，也就是走 `steam://rungameid/<appid>` 启动游戏。
  > 直接把 `steam://rungameid/431960` 这样的裸 URL 填进配置也能启动，
  > 但那条路**没有图标**（Shell 对裸协议 URL 不提供图标）、也没有「打开文件所在位置」，
  > 所以推荐走 `.url` 快捷方式。
- **最左侧的「临时文件夹」**：一个**真实目录**（`%LOCALAPPDATA%\dev.local.dock\临时文件`），
  点一下就在资源管理器里打开 —— 拿来放临时文件。它就是个普通条目（能拖、能移出 Dock），
  移出去之后从**托盘 →「添加临时文件夹」**再加回来（会重新放回最左侧）。
  想换个位置就说一声（路径在 `apps::temp_folder_path`，一行的事）。
- **右键文件夹** → 「**重命名…**」（弹输入框，默认是它现在的名字）/「解散文件夹」/「移出 Dock」。
- **右键回收站** → 「**清空回收站**」（红字；**空的时候置灰**）。
  ⚠️ 确认框是 **Windows 自己弹的**（我们刻意不传 `SHERB_NOCONFIRMATION`）——
  这是不可撤销的操作，不该由我们替你决定。清空后图标会立刻变成"空"的那张（见 BL-8）。
- **右键 Dock 上的图标** → 激活 / 最小化 / 关闭 / **从 Dock 移除** / 以管理员身份运行 / 打开文件所在位置。
  右键里还有 **「新建文件夹…」**（会弹输入框让你填名字，见下）、「添加分割线」。
- **大文件夹**：右键 →「新建文件夹…」→ **填名字**（默认「新建文件夹」）→ 原图标原位变成文件夹，
  自己成了里面第一个；把别的图标**拖到文件夹上**就放进去了。点文件夹展开网格，
  里面的图标可以点开、可以右键「移出文件夹」（**最后一个移出后文件夹自动解散**，
  不再留一个点不开的空壳）。
- **右键托盘图标** → 添加应用… / 添加系统位置 / **添加临时文件夹** / **设置…** / 自动隐藏 / 开机自启 / 退出 Dock。
- **设置窗口**（托盘 →「设置…」）是**侧边栏分栏**式，四栏：
  **外观**（实时 Dock 预览 + 色调 + 浓度 + 图标边长 + **面板高度** + 悬停放大）、
  **位置**（屏幕示意图 + 距屏幕底边 + 给任务栏留空）、
  **行为**（自动隐藏 + 收起延迟 + 运行指示点）、
  **信息**（只读的运行时状态）。
  改完都**立刻生效**，不需要重启 —— 开着设置页就能看到 Dock 跟着变。

## 开发循环

```powershell
# 前端（用 fnm 的 node 绝对路径；DSH 自带的 node 没有 npm）
$nodeDir = "C:\Program1\fnm-window\node-versions\v24.19.0\installation"
$env:PATH = "$nodeDir;$env:PATH"
$env:npm_config_cache = "C:\Program1\Projects\Dock\.npm-cache"

cd app
npm run check:magnify     # 鱼眼放大的纯数学校验（14 项断言）
npm run build             # 类型检查 + 打包到 app/dist

cd src-tauri
cargo build --offline     # ← 必须！Tauri 在**编译期**把 dist 嵌进二进制
```

> **最容易踩的坑**：改完前端只跑 `npm run build` 是**没用的**，
> 必须再 `cargo build` —— 否则应用还在跑旧前端，现象是「改了没反应」。

### 两个可复跑的脚本（别再手搓一次性命令）

| 脚本 | 干什么 | 为什么值得留下 |
|---|---|---|
| `scripts/selftest.ps1` | **验收闸**：构建 selftest 版 → 在**配置副本**上跑全套（焦点矩阵 / 层级 / 拖拽 / 菜单+大文件夹 / 系统位置）→ 报告失败项、核对**真实配置没被改动** → 自动回做产品构建 | 这套流程有五个坑：要用配置副本、`--features selftest` 会**覆盖**产品二进制、**构建前必须先杀掉运行中的 Dock**（否则文件被占用构建失败）、光标要先挪开（否则任务栏弹出来盖住 Dock，所有需要真实输入的检查全挂）、**Dock 必须真的露在桌面上**（它刻意不置顶；而且 2026-09 起层级钩子会拦掉任何把它顶起来的尝试，所以"用 `HWND_TOPMOST` 顶一下再跑"这招失效了 —— 请 `Win+D` 或最小化遮挡窗口） |
| `scripts/soak.ps1` | **长稳采样**：每 120 秒把 Dock 本体 + 它的 WebView2 子进程的内存/句柄/线程/CPU 追加到 `docs/soak-log.txt` | 「连续跑 8 小时不崩」这条验收只能靠真实流逝的时间，采样得有人守着记 |

```powershell
pwsh -File scripts/selftest.ps1        # 退出码 0 = 全过
pwsh -File scripts/soak.ps1 -Once      # 先看看它采出来的是什么
pwsh -File scripts/soak.ps1            # 后台常驻，正常用电脑就行
```

### 环境变量

产品构建可用的：

| 环境变量 | 作用 |
|---|---|
| `DOCK_LOG=error\|warn\|info\|debug\|trace` | 日志级别，默认 `info`。`debug` 才记枚举明细/图标请求这类刷屏细节（等价于老的 `DOCK_DEBUG=1`） |
| `DOCK_LOG_DIR=<dir>` | 重定向日志目录（默认 `%LOCALAPPDATA%\dev.local.dock\logs`；自检时跟着 `DOCK_CONFIG_DIR` 走） |
| `DOCK_CRASH_TEST=panic\|av` | **故意崩一次**，用来验证崩溃取证链路本身（`panic` → panic 钩子；`av` → 访问违例 + Minidump）。只在 debug 构建生效 |
| `DOCK_DEBUG=1` | 兼容老开关：等价于 `DOCK_LOG=debug`（打印窗口枚举明细）。⚠️ **开销明显**，仅排查用，别常开 |
| `DOCK_OPENSETTINGS=1` | 启动就把设置窗口打开并留着（正常走托盘「设置…」）。用于核对设置页的排版观感 |
| `DOCK_CONFIG_DIR=<dir>` | 重定向配置目录（配置只需写要改的字段，其余由 `serde(default)` 补齐） |
| `DOCK_WEBVIEW_DATA=<dir>` | 重定向 WebView2 数据目录（开发用；生产不设，走 `%LOCALAPPDATA%\{identifier}`） |

### 日志与崩溃取证

**Dock 没有控制台**（GUI 子系统，双击启动不会弹终端窗口），所有输出都进文件：

```
%LOCALAPPDATA%\dev.local.dock\logs\
├── dock-2026-09-25.log          当天的日志（按天一个文件；超 8MB 轮转成 .1.log；保留 7 天）
├── crash-20260925-100944.txt    崩溃报告（异常码 / 地址 / 归属模块 + 崩溃前 40 行日志）
└── crash-20260925-100944.dmp    Minidump（VS / WinDbg 直接打开，带完整调用栈；保留最近 20 份）
```

日志长这样（时间 + 级别 + **模块路径** + 内容，`grep` 友好）：

```
2026-09-25 10:11:11.172 INFO  [dock_app::store] [配置] 已加载 C:\Users\…\config.json
2026-09-25 10:11:11.724 INFO  [dock_app::logging] [前端 dock] 页面已加载（/）
2026-09-25 10:11:13.542 INFO  [dock_app::logging] [前端 panel] 页面已加载（/panel.html）
2026-09-25 10:20:31.412 ERROR [dock_app::store] [配置] 新建文件夹失败: …
2026-09-25 10:35:00.001 INFO  [dock_app::logging] [心跳] 已运行 20 分钟，工作集 47 MB
```

**出问题时按这个顺序看**：

1. **先看有没有 `crash-*.txt`**（时间戳对得上就打开它）—— 里面第一屏就写着
   异常码（如 `0xC0000005 ACCESS_VIOLATION`）、出错地址、**落在哪个模块 + 偏移**
   （`dock-app.exe+0x32E2ED` / `nvoglv64.dll+0x1234`），后面贴着**崩溃前的最后 40 行日志**，
   通常一眼就能看出"崩在哪一步"。要更细的栈就打开同名 `.dmp`（VS 里"打开转储文件"即可，
   PDB 就在 `target\debug\` 旁边，符号能直接解析到 Rust 源码行）；
2. **没有 crash 文件但 Dock 没了** = 不是异常而是**被结束**（托盘退出、任务管理器、
   单实例抢占）。日志最后一条**心跳**的时间就是死亡时刻 —— 心跳每 5 分钟一行；
3. **Rust 侧 panic**（`unwrap` 失败等）会在日志里留 `💥 PANIC` + 位置 + 线程 + **符号化回溯**；
   这类不会生成 crash 文件（进程按 Rust 规则退出，退出码 101）；
4. **页面白屏 / 图标不显示**：看 `[前端 …]` 开头的行 —— WebView 是独立进程，
   页面的未捕获异常、`console.error`、React 渲染报错都由前端转发过来（`src/lib/log.ts`）。

验证这套链路本身：`DOCK_CRASH_TEST=panic`（或 `av`）启动一次即可 —— 实测 panic 会留下
`💥 PANIC` + 回溯；`av` 会留下 `crash-*.txt`（本次实测：`0xC0000005`、写地址 `0x0`、
归属 `dock-app.exe+0x32E2ED`）+ 645 KB 的 `.dmp`。

自检脚手架 —— **必须 `cargo build --features selftest`**，产品构建里这些代码根本不存在：

> **自检只动它自己造的东西。** 它会在**你的真实配置的副本**上跑，最后要还原成原样 ——
> 所以 `restore_separators` 会**跳过** `original` 里已经存在的分割线与文件夹
> （用户可能本来就有文件夹，甚至有好几个同名的「新建文件夹」）。
> 早期版本"把所有文件夹都解散"，遇到这种配置会把用户的文件夹拆掉、里面的条目甩到顶层，
> 于是「测试结束后列表已还原」必然报红（实测 27 项被搞成 48 项）。
> 同理，涉及"移除了没有"的断言一律**按 id 判**，不按显示名 —— 名字不保证唯一。
>
> **两条已经踩过的自检坑**（都改成了"断言结果、容忍环境"）：
> 1. **合成点击偶尔会被吃掉一次**。实测"点外面关菜单"多数一次就成，偶尔第一次无效
>    （同一次运行里 Esc 那条路每次都准 → 分层逻辑没问题，是注入的问题）。
>    所以那两条断言改成"最多点两次，但**结果必须**是菜单关了、文件夹还开着"，
>    并把实际点了几次写进输出 —— 真正的回归（一次把两层都关掉）照样会被抓住。
> 2. **"本次含在跑的应用"不能当硬要求**：拖进文件夹的那个应用在不在跑取决于用户当时开着什么。
>    改成"一致性永远断言；有在跑的就要求点亮运行点，没有就只验一致性"并写明本次是哪种。

| 环境变量 | 作用 |
|---|---|
| `DOCK_SELFTEST=0` | 跳过焦点矩阵自检（默认会跑，会建辅助窗口） |
| `DOCK_LAYERTEST=0` | 跳过**层级自检**（默认跑：不用合成点击、不用辅助窗口，所以在任何环境下都能验证"Dock 不置顶 + 就在最底层"） |
| `DOCK_OPSTEST=1` | 额外跑「应用操作 + IPC 链路 + 设置窗口 + 拖放添加」自检 |
| `DOCK_DRAGPROBE=1` | 跑「指针拖出窗口后事件还在不在」实验（BL-4 前置，会合成一次鼠标拖拽） |
| `DOCK_DRAGTEST=1` | 跑 Dock 内拖拽自检（排序 / 拖出删除 / 非法顺序拒绝，**会临时改动并还原配置**，所以**必须同时设 `DOCK_CONFIG_DIR`**，否则直接拒绝跑） |
| `DOCK_MENUTEST=1` | 跑浮层（右键菜单 + 大文件夹）+ 分割线自检（**会临时改动并还原配置**，所以**必须同时设 `DOCK_CONFIG_DIR`**，否则直接拒绝跑） |
| `DOCK_MENUSHOT=<秒>` | 打开浮层并保持一段时间，用于**截图核对观感**（第一个条目是文件夹就展开文件夹，否则弹菜单；配 `DOCK_SELFTEST=0` 用） |
| `DOCK_LOCTEST=1` | 跑系统位置自检（**会真的开两个资源管理器窗口**验证「此电脑 / 回收站」能打开，跑完自动关掉） |
| `DOCK_ICONBOX=1` | 量「图标间距与图形留白」：布局间距 / 图标原始留白 / 渲染后的视觉间距（排查"某几个图标显得空"） |
| `DOCK_LAUNCH_TEST=<exe>` | 自检里额外验证启动（会真的拉起一个进程） |
| `DOCK_DUMP_APPS=1` | 启动后打印枚举到的应用（含图标提取结果与 alpha 统计） |

> `DOCK_GLASS=a|b|none` 已删除 —— 路线 A 实测在真实 Tauri 窗口上无效（见
> `p1-window-layer-findings.md` §6.6），代码和这个开关一起删掉了，现在只有亚克力一条路。

## 换台机器能直接用吗（全是现查的）

**这是为开源专门过了一遍的原则：代码里不留"只在我这台机器上成立"的东西。**
早期版本确实有（把 Edge 的安装路径、几个打包应用的 AUMID 和一批中文应用名写死在
`store.rs` 的预置表里），现在全部换成**探测**：

| 原来写死的 | 现在怎么来 | 换机器为什么成立 |
|---|---|---|
| `C:\Program Files (x86)\Microsoft\Edge\…` | 查注册表 **`App Paths`**（HKCU→HKLM、64 位视图→`WOW6432Node` 都查一遍），再验存在性 | 微软安装规范要求所有 GUI 程序登记它，值与安装位置无关；装到 D 盘也找得到 |
| `%WINDIR%\explorer.exe` | `SystemRoot` 环境变量 | 系统盘不一定是 C: |
| 打包应用的 AUMID（本机 `Get-StartApps` 抄的） | **包族名前缀**（`Microsoft.WindowsTerminal_8wekyb3d8bbwe`）+ `target_exists` 现查 | 发布者哈希由发布者决定，所有机器相同；没装就自动跳过 |
| 中文显示名（"终端"/"记事本"/"设置"） | **Shell 显示名**（打包应用）或**文件版本信息**（exe） | 英文系统上自动是 `Windows Explorer` / `Microsoft Edge` |
| —— | `WindowsApps\` 下的路径一律不预置 | 那个目录 ACL 只允许经 AppsFolder 激活，直接启动会被拒绝（等于点不开的图标） |
| 屏幕尺寸 / DPI / 任务栏高度 | `rcWork` + DPI + `ABM_GETTASKBARPOS` 现算 | 分辨率、缩放比、任务栏位置都变了也跟得上 |

**首次运行会预置什么**（本机实测，一台装了 Edge+Chrome+终端的机器）：

```
此电脑 · 回收站 · Windows 资源管理器 │ Microsoft Edge · Google Chrome · 终端 · 设置
```

没装的应用**不会**出现（所以不会留下点不开的空图标）；刻意不预置记事本/计算器/画图那种
小工具 —— 一上来塞 8 个图标会让人第一时间就想删。**已经配置过的用户不受影响**：
预置只在 `config.json` 不存在时跑一次。

### 发布前检查清单

- [x] 代码里没有本机绝对路径 / 用户名 / 盘符（`grep -rn "C:\\\\Users\|D:\\\\" app/src-tauri/src` 只应命中测试数据）
- [x] 预置不走硬编码（有单测 `default_pin_rules_are_machine_independent` 钉住）
- [x] 显示名不写死中文（跟随系统语言）
- [x] 日志/崩溃文件写在 `%LOCALAPPDATA%`，不进仓库（`.gitignore` 也兜住了）
- [x] 没有 `.cargo/config.toml` 之类的国内镜像配置（那会让别人 `cargo build` 直接失败）
- [x] `Cargo.lock` / `package-lock.json` 都提交（二进制项目要锁版本）
- [x] LICENSE 已加（**MIT**，署名 `EvanLofton`；要换协议或改署名就改这一份）
- [x] README 有实拍截图（`docs/images/dock.png`）
- [x] 应用图标是自己画的（`scripts/make-icon.ps1`，纯 System.Drawing，**不依赖任何外部素材** ——
      把网上找的图塞进 MIT 仓库是版权坑；想换风格改脚本重跑即可，产物是
      `icons/icon.png` + 多尺寸 `icons/icon.ico`）
- [ ] GitHub 的 **Social preview**（仓库设置里单独传一张 1280×640 图）—— 这个只能在网页上设
- [ ] UI 文案目前**只有中文**（这是早先"不做 i18n"的决定）。要有英文界面得另开一轮工作

## 配置参考

配置在 `%APPDATA%\dev.local.dock\config.json`（`DOCK_CONFIG_DIR` 可重定向）。

**改配置文件需要重启；从托盘或设置页改的项立刻生效。**

| 字段 | 默认 | 说明 | 怎么改 |
|---|---|---|---|
| `pinned` | 出厂预置若干 | **Dock 上有哪些图标、按什么顺序** —— 唯一真相。里面也放**分割线**（`{"separator": true}`）、**系统位置**（此电脑 / 回收站）、**大文件夹**（`{"isFolder": true, "children": [...]}`）—— 三者都是"列表里的普通条目"。target 可以是 exe 全路径、`shell:AppsFolder\<AUMID>`、**`.url` 快捷方式路径**（Steam 游戏就是这么放的） | 从桌面拖入 / 托盘「添加应用…」/ 托盘「添加系统位置」/ 右键菜单（新建文件夹 · 加分割线 · 移出 Dock）/ Dock 内拖拽排序 / 把图标拖到文件夹上 |
| `bottomOffset` | `0` | Dock 底边距基准底边多少**逻辑像素**（0 = 贴底） | 设置页 · 位置 |
| `glassRgb` | `[24,24,28]` | 背景色调。同时决定 OS 亚克力染色与前端遮罩 | 设置页 · 外观 |
| `glassAlpha` | `150` | 背景浓度（30–255）。越大越「实」、越看不见模糊 | 设置页 · 外观 |
| `iconSize` | `44` | 图标边长（逻辑像素，32–72） | 设置页 · 外观 |
| `iconGap` | `10` | **图标之间的间距**（逻辑像素，0–30）。这是"方格"间距；视觉间距还会按图形留白做光学补偿 | 设置页 · 外观 |
| `panelHeight` | `72` | **面板高度**（逻辑像素）。窗口高度与它一致；有几何下限（要装得下图标，前端会兜底抬高），上限 200 | 设置页 · 外观 |
| `magnification` | `0.35` | 鱼眼放大增量（上限由面板高度算出，超了会被收敛） | 设置页 · 外观 |
| `autoHide` | `false` | 自动隐藏（贴底唤出） | 设置页 · 行为 **或** 托盘勾选 |
| `hideDelayMs` | `420` | 自动隐藏前鼠标要离开多久（100–2000） | 设置页 · 行为 |
| `showRunningIndicators` | `true` | 是否显示运行白点 | 设置页 · 行为 |
| `reserveTaskbar` | `true` | 给任务栏留空间。**任务栏自动隐藏时也留** —— 问的是 `ABM_GETSTATE` 的「配置」而不是「此刻弹没弹出来」，高度取 `ABM_GETTASKBARPOS` 的吸附矩形（见「已知限制」里那条） | 设置页 · 位置 |
| `launchAtLogin` | `false` | 开机自启 | **只在托盘**（要写注册表，不走 `set_preferences`） |

> **改配置文件需要重启；从设置页或托盘改的项全都立刻生效**（`commands::apply_live` 会重应用
> 亚克力、重算位置，并用 `eval` 通知 Dock 页面重读配置）。自检里逐项验过：
> 背景色、距底部、**图标边长、面板高度**都是即时生效。
>
> **面板变高时窗口往上长、底边不动** —— 否则贴底的 Dock 会长到屏幕外去。自检里同时断言了
> 「高度增加了应有的量」和「底边坐标没变」两点。

> 配置文件里**没有** `discovered` 之类的历史字段 —— 旧版本的两段式模型（固定项 + 自动出现的
> 运行中应用）已废弃，见 `docs/dock-technical-design.md` §4.2「应用顺序模型」。

## 目录结构

```
Dock/
├─ app/                         Tauri 应用
│  ├─ index.html                Dock 本体的页面入口
│  ├─ settings.html             设置窗口的页面入口（Vite 多页构建）
│  ├─ panel.html                浮层入口：右键菜单 + 文件夹面板（Vite 多页构建）
│  ├─ src/                      React 前端
│  │  ├─ lib/magnify.ts         鱼眼放大纯数学（可单测）
│  │  ├─ lib/ipc.ts             类型化 invoke 封装 + 图标 canvas 编码缓存
│  │  ├─ components/Dock.tsx    图标行（transform-only 放大）
│  │  ├─ panel/                 浮层页面（纯渲染器：菜单项 / 文件夹内容 / 动作都在 Rust）
│  │  └─ settings/              设置页（基础信息 + 背景色）
│  └─ src-tauri/src/
│     ├─ main.rs                入口、单实例、线程编排、窗口层初始化
│     ├─ win_layer.rs           窗口样式 / 焦点防护 / DPI 工作区定位
│     ├─ glass.rs               毛玻璃（亚克力）与窗口材质
│     ├─ reveal.rs              自动隐藏状态机
│     ├─ apps.rs                窗口枚举 / 身份解析 / 窗口操作 / 启动
│     ├─ icons.rs               图标提取 + 预乘 alpha 修正
│     ├─ commands.rs            前端唯一能触达平台层的入口（Tauri 命令）
│     ├─ drop.rs                从桌面拖程序到 Dock（路径解析 / 去重 / 落盘）
│     ├─ settings_window.rs     设置窗口的创建与前置
│     ├─ model.rs               Dock 条目的纯数据结构（含 `separator` / `isFolder` + `children`）
│     ├─ menu.rs                右键菜单的**内容与动作**（纯逻辑，可单测）
│     ├─ panel_window.rs        浮层**窗口**：右键菜单 + 文件夹面板共用（创建 / 定位 / 显示隐藏 / 看门线程）
│     ├─ picker.rs              「添加应用…」文件对话框
│     ├─ tray.rs                系统托盘
│     ├─ store.rs               配置持久化 + 首次运行预置常用应用
│     ├─ autostart.rs           开机自启（HKCU Run）
│     └─ selftest.rs            焦点矩阵 / 应用操作 / IPC / 设置窗口 / 拖拽 / 菜单自检（`--features selftest`）
├─ probe/                       P0 验证探针（独立于应用，可复跑）
└─ docs/
   ├─ dock-technical-design.md  技术方案（v5，含全部实测修正）
   ├─ p0-report.md              P0 可行性验证报告
   ├─ p1-window-layer-findings.md  P1 逐轮实测发现（**最重要的一份**）
   ├─ backlog.md                后期功能更新（已知道要改但现在不做的，附前因后果）
   └─ soak-log.txt              长稳采样日志
```

## 已自动化验证的行为（附证据位置）

| 行为 | 验证方式 | 证据 |
|---|---|---|
| 毛玻璃真的模糊（路线 B） | 锐利边缘法量亮度剖面 | `p0-report.md` §2、`p1-...` §6.6 |
| **点击 Dock 不夺走当前应用焦点**（跨进程、真实工况） | 六相位焦点矩阵，含双向阳性对照 | `p1-...` §6.14 |
| 激活 / 最小化 / 还原 / 启动 | 跨进程窗口断言，**最终 6/6 通过** | `p1-...` §6.15、§6.17 |
| 固定 / 取消固定落盘 | 写出后读回磁盘校验 | 自检 ⑥ |
| 设置窗口：能开、页面渲染、IPC 通、**背景色 / 距底部 / 图标边长 / 面板高度都即时生效** | 窗口扩展样式断言 + `eval` 注入页面改值再读回（图标边长读 Dock 页面里第一个图标的 `style.width`；面板高度断言窗口变高**且底边不动**） | 自检 `drive_settings_test` |
| 拖放添加：exe 被接受且排末尾、重复被去重、文件夹/未知扩展名被拒、`.lnk` 能解析、运行中判定正确、测试后还原 | 直接调用拖放处理函数并断言固定列表 | 自检 `drive_drop_test`（7/7） |
| Dock 内拖拽排序 / 拖出删除 / 拖回不删 / 松手不启动 / 拖出时有文字提示 / 非法顺序被拒 | 在真实窗口上合成鼠标拖拽（含拖到一半读 DOM 里的提示文字），断言**落盘的顺序**与列表 | 自检 `drive_drag_test`（7/7） |
| **分割线**：插在指定项后面、落盘、渲染成竖线（按钮宽 9、线本身 1px 浅色无描边、无图标无白点）、行宽精确 +7 | 直接调用 `store::add_separator` 后读 DOM（`getBoundingClientRect` + 计算样式）与配置 | 自检 `drive_menu_test` |
| **右键菜单是独立窗口**：右键图标后窗口出现、贴在 Dock 上方、以光标为中心（实测差 0px）、**不抢前台焦点**、菜单项与 Rust 一致、内容不溢出（`scrollHeight ≤ clientHeight`） | 真实合成右键 + 量窗口矩形 + 读菜单页面 DOM | 自检 `drive_menu_test` |
| **菜单的显示/隐藏**：同一个图标再右键 = 关掉、Esc 关闭、点别处关闭、点菜单项关闭且动作真的执行（加分割线 / 移除分割线） | 合成右键 / 合成 Esc / 合成点击，断言窗口可见性与配置变化 | 自检 `drive_menu_test`（21/21） |
| 指针拖出 Dock 窗口后事件仍在送达 | 注入探针记录每个 pointer 坐标（到窗口上方 158px） | 自检 `drive_drag_probe` |
| 前端能调到 Rust 命令 | WebView eval + 回执命令 | `p1-...` §6.15 |
| 自动隐藏全循环 | 逐帧读窗口矩形 | `p1-...` §6.9 |
| 与自动隐藏任务栏零重叠 | 量 `Shell_TrayWnd` 几何 + `ABM_GETTASKBARPOS` 的吸附矩形；把光标压到屏幕最底边逼任务栏弹出，再量 Dock 与「光标位置最上层窗口」 | `p1-...` §6.16；**2026-09 修了一个真 bug**（见「已知限制」下的自动隐藏任务栏那条）：修后 Dock `(453,950)-(1466,1020)`、任务栏 `1020-1080`，弹出后图标行中心的最上层仍是 `Dock`，任务栏区内一点是 `Shell_TrayWnd` |
| 配置真的驱动行为 | 改 config 重启后量行为 | `p1-...` §6.10 |
| 界面渲染与居中 | 窗口像素网格量测 | `p1-...` §6.7 |
| 鱼眼放大数学（含**分割线不放大**：倍率恒为 1、邻居照常让位、行宽按 SEP_W 计） | 20 项纯函数断言 | `app/scripts/check-magnify.mjs` |
| 菜单项构成：分割线只有「移除分割线」、未运行时给「启动」、运行中给三个窗口操作、已提权置灰 runas、打包应用不能「打开文件所在位置」、宽度钳制 | 10 项 Rust 单测（纯逻辑，不碰系统） | `cargo test --offline` |
| **系统位置（此电脑 / 回收站）**：名字取系统语言、图标真取得到且不是一块底板 | 2 项 Rust 单测（含 COM 初始化；断言图标 ≥32px 且透明像素 >20%） | `cargo test --offline`（实测「此电脑」「回收站」128×128，透明 41~42%） |
| **大文件夹**：右键「新建文件夹」原位变文件夹、点图标展开浮层、拖图标进去、解散回顶层、**里面悬停也有名字气泡** | 真实交互（合成右键 / 左键 / 拖拽 / 悬停），断言配置结构变化 + 浮层可见性与位置 | 自检 `drive_menu_test` 的「大文件夹」段（19/19） |
| **运行点归属**：文件夹自己**没有**白点；里面在跑的那一格**有**（永远是白点，不随主题变黑） | 读两处 DOM（Dock 图标 / 面板格子）与 Rust 给的运行态对比 | 自检 `drive_menu_test`（实测 页面 `[false, true]` = 运行态） |
| **点文件夹里的图标真的会打开** | 合成点击后断言资源管理器窗口数增加（拿「回收站」当被测对象），只关掉测试开的那个 | 自检 `drive_menu_test`（实测窗口 0 → 1） |
| **面板里右键弹的是它自己的菜单**（不是浏览器默认菜单） | 合成右键后读菜单项，断言含「移出文件夹」 | 自检 `drive_menu_test` |
| **两层浮层逐层关闭**：在文件夹里右键 → 菜单和文件夹同时开着；点外面先关菜单、再点才关文件夹；Esc 同理 | 每一步都读**两个层**的可见性（菜单 / 文件夹） | 自检 `drive_menu_test` |
| 「移出文件夹」= 挪到顶层，不是删掉 | 合成点击后断言：文件夹少一项、顶层多一项、且紧跟在文件夹后面 | 自检 `drive_menu_test` |
| **浮层 payload 的字段名**（页面读 `rowH` / `dividerH` …） | 单测直接断言序列化出来的 JSON 键名 | `cargo test`（这条是"菜单行高失效"那个 bug 的回归闸） |
| **浮层不是弹窗**：贴 Dock 上方（面板底 874 ≤ Dock 顶 884）、同材质、不抢焦点、Esc/点别处关闭 | 量窗口矩形 + 前台窗口断言 | 自检 `drive_menu_test` |
| **系统位置点得开** | `DOCK_LOCTEST=1`：真的调 `apps::launch`，断言出现了新资源管理器窗口（实测标题「此电脑 - 文件资源管理器」），**只关掉刚开的那个** | 自检 `drive_location_test`（4/4） |
| **系统位置不被误认成打包应用**（`shell:AppsFolder\…` 是应用、`shell:MyComputerFolder` 是位置） | 单测断言菜单第一项是「打开」且 runas 置灰 | `cargo test --offline` |
| **`.url` 快捷方式（Steam 游戏）**：认得出、读得出 `IconFile=`、空壳被拒且理由具体、target 保留快捷方式路径 | 单测用**临时构造**的 `.url`（含 `%WINDIR%` 需要展开的 `IconFile`）跑三条断言，不依赖本机装没装 Steam | `cargo test --offline`（`url_shortcut_is_recognized_and_its_icon_source_resolved`） |
| **`.url` 的图标是游戏图标，不是通用「Internet 快捷方式」图标** | 拿真实的 `Wallpaper.url` 跑端到端：临时配置里加一条 → 起 Dock → 量末尾图标的位图哈希与截屏 | 实测 `extract_icon(.url)` 与 `extract_icon(launcher.exe)` **位图哈希完全相同**（`C12E1A441BB7C5E5`），截屏确认没有快捷方式小箭头 |
| **接管任务栏背景（已撤掉）** | 见下方「已知限制」里那条 —— 实测那条路只能让任务栏**更实**，做不到更透；Win11 上的完全透明需要注入 explorer，本项目不做。全部证据在 `docs/dock-technical-design.md` §4.1.3 | 撤销前实测：开 a=120 → 52.5% / 17.7；a=0 → 11.6% / 6.5；a=255 → 69.5% / 31.9；关掉重启 → 0% / 1.5（还原是干净的，但方向错了） |
| **自检不会破坏用户配置**：只清它自己造的分割线/文件夹，用户原有的（含同名、含文件夹）一个不碰 | 拿**用户真实的 27 项配置（含 4 个同名「新建文件夹」、共 21 个子项）**跑整套，最后断言列表与运行前逐 id 一致 + 真实配置 SHA256 不变 | 修前：3 项报红（`27 项 → 48 项，一致 = false`）；修后：**7/7 + 40/40 + 4/4 全过**，`一致 = true`，真实配置未被改动 |
| **图标视觉间距一致**（布局间距一致 ≠ 看起来一致） | `DOCK_ICONBOX=1` 一次量三层：布局间距 / 图标原始留白 / 渲染后的留白与视觉间距 | 实测全部 **10.0**（修前：17.1 · 14.8 · 10.0） |
| **层级：Dock 只在桌面层，浮层永远最上** | `WindowFromPoint` 取 Dock 矩形中心点的最上层窗口：无遮挡 / 被普通窗口盖住 / 遮盖窗口改 TopMost / 关掉遮盖 —— 四相；再读三个窗口的扩展样式 | 实测 `'Dock'` → `'ZTEST-COVER'` → `'ZTEST-COVER'` → `'Dock'`；`Dock` `EX=0x8000190 TOPMOST=False`，`Dock 浮层`/`Dock 文件夹` `EX=0x8000198 TOPMOST=True` |
| **外部把 Dock 设成置顶：从未生效**（2026-09 用户报的"开游戏时 Dock 浮在游戏上面"） | 两条路都试一遍：`SetWindowPos(HWND_TOPMOST)` 与直接改 `GWL_EXSTYLE` 加 `WS_EX_TOPMOST`；断言样式位**当场**没变（不是过一会儿被纠正）、且钩子计数确实涨了（证明是"拦截"而非"碰巧"） | 自检 `drive_layering_test` **7/7**；实测 `ex=0x8000190` 全程不变，计数涨；`GetWindowLongPtrW` 成本实测 **8.5 ns/次**（所以第一版的 1Hz 轮询问题在**延迟**不在性能，已改成消息拦截） |
| **Dock 就在最底层**（除桌面外没有窗口排在它后面） | 启动时 `SetWindowPos(HWND_BOTTOM)` 沉一次底（**在装层级钩子之前**，之后谁也抬不动）；自检用 `EnumWindows` 下标判断"排在 Dock 后面的可见窗口必须为空"（纯枚举，不需要点击） | 实测 Dock 下标 **132**、桌面 `Progman` **133**、其余窗口 0~126 —— 只有桌面在它下面，且**没掉到壁纸后面**；自检 `drive_layering_test` 里那条 `Dock 下标 127；排在它后面的可疑窗口 0 个` |
| **explorer 重启把桌面插到 Dock 上面 → 自动沉回去** | 每 2 秒扫一次 Z 序（`win_layer::recover_if_desktop_above`）：发现有**可见**桌面系窗口（`Progman`/`WorkerW`/`SHELLDLL_DefView`）排在 Dock 之前，就把**那些窗口**沉到底 —— **Dock 自己的 Z 序一次都不动**，所以"永不上升"依旧绝对、钩子零旁路 | 实测（把一个隐藏的 `WorkerW` 显示出来造故障）：WorkerW 下标 `34 → 88`、Dock `91 → 87`，3 秒内自愈；日志 `[层级] 桌面窗口跑到了 Dock 上面…已把桌面沉回 Dock 下方`；自检 `drive_layering_test` 8/8（含"桌面没有压在 Dock 上面"这条） |
| **悬停名字不是原生 tooltip**：Dock 图标和**大文件夹里的格子**都一样 —— 自己画的气泡出现在**图标正上方**，带 120ms 延迟、拖拽中不弹、分割线上不弹；底色**单独提亮**（浅色磨砂底 + 深色字，不是中灰玻璃压白字） | 用**真实合成鼠标**逐个图标扫过去，量浮层窗口矩形与 Dock / 文件夹面板矩形的关系、并截屏量底色亮度；代码里已经没有 `title`（全部换成 `aria-label`） | 实测（1013×70 的 Dock）：x=620 → 气泡 `(555,911)-(694,940)` 139×29；底边恒为 940 = Dock 顶 950 − 8 逻辑像素；底色亮度 **147 → 191**（提亮前白字对比不到 2:1）；文件夹里 `label_visible=true 气泡 (357,806)-(431,835) 面板顶 845 水平偏差 0px`（自检 `drive_menu_test` 也把这条立成了闸） |

最终验收自检输出（`DOCK_OPSTEST=1`）：

```
[PASS] 前置：焦点在别处
[PASS] activate() 把目标带到前台
[PASS] minimize() 让目标最小化
[PASS] activate() 能把最小化窗口还原并带到前台
[PASS] launch() 后运行中的应用数增加        运行中应用数 3 -> 4
[PASS] 固定 / 取消固定能持久化到磁盘        写盘=true 清除=true
小结：6 项通过，0 项失败
回执: activate_app:ok   目标是否到前台: true   -> 链路完整 ✅
```

右键菜单 / 分割线 / 大文件夹自检输出（`DOCK_MENUTEST=1`，节选）：

```
[PASS] 页面就绪：窗口宽度已按面板尺寸上报（不是建窗初值）  窗口宽 1013 物理像素，17 个图标
[PASS] 行宽增加 = 分割线宽 9 + 两侧紧间距 4+4 − 被顶掉的间距  行宽 762 → 771（+9，期望 9.7）
[PASS] 线本身：1px 宽、有高度、用浅色（不是会融进底色的中性灰）  宽=1 高=26 颜色=rgba(255, 255, 255, 0.42)
[PASS] 右键图标 → 独立菜单窗口已显示  is_visible=true  菜单矩形 (392,710) 210x230
[PASS] 菜单贴在 Dock **上方**且在屏内  菜单底 940 ≤ Dock 顶 950
[PASS] 菜单以光标为中心水平锚定  菜单中心 497 vs 光标 497（差 0 物理像素）
[PASS] 菜单没有抢走前台焦点  前台 0x5037A 'debug - 文件资源管理器' → 同一窗口
[PASS] 菜单高度没有溢出（Rust 的尺寸算法与页面渲染一致）  scrollHeight=184 ≤ clientHeight=184
[PASS] 同一个图标再次右键 → 菜单关闭（右键是开关）
[PASS] Esc 关闭菜单 / 点菜单和 Dock 以外的地方 → 菜单关闭
[PASS] 点「添加分割线」真的在那一项后面加了一条  18 项 → 19 项
[PASS] 点「移除分割线」把它从 Dock 上删掉了  19 项 → 18 项
[PASS] 文件夹里悬停一格 → 名字气泡出现在**面板上方**（不是原生 tooltip）  气泡 (357,806)-(431,835) 面板顶 845 水平偏差 0px
[PASS] 点文件夹里的图标 → 真的打开了（不是点了没反应）  「回收站」资源管理器窗口 1 → 2
[PASS] 「移出文件夹」把它挪到**顶层**（文件夹后面），不是删掉
小结：40 项通过，0 项失败
```

## 已知限制（不是 bug，是平台或刻意取舍）

| 限制 | 原因 |
|---|---|
| **同进程点击 Dock 会夺焦点** | `WM_MOUSEACTIVATE` 落到无法子类化的深层 Chromium 窗口（comctl32 线程局部，硬限制）。实际防护是「**Dock 窗口**不创建任何会获得焦点的东西」。⚠️ 自检里 `[A]`/`[B]` 两相报红就是这个已知限制，**验收判据是 `[C]` 跨进程相**（真实工况）。注意这条只针对 Dock 窗口 —— **设置窗口是普通可聚焦窗口**，那是应该的（要打字、拖滑块），见 `settings_window.rs` |
| 独占全屏游戏内看不到 Dock | Windows 机制限制，`TOPMOST` 压不过 D3D 独占全屏 |
| **自动隐藏的任务栏**：留空必须问「配置」而不是看「此刻的位置」 | 任务栏自动隐藏时 `rcWork` **恒等于整屏**，但任务栏一触底就会弹出来盖住 Dock。旧写法靠"任务栏窗口顶边是否贴着屏幕底"判断它是不是自动隐藏 —— 任务栏**弹出来的那一瞬间**顶边回到 1020，看着和常显一样，于是判断成"不用留空"，Dock 就永久压在弹出区上（实测 Dock 底 1080、任务栏 1020-1080，鼠标移到 Dock 上等于把任务栏叫出来盖住自己）。现在用 `ABM_GETSTATE` 问「是不是配置成自动隐藏」、用 `ABM_GETTASKBARPOS` 拿吸附矩形的高度 —— 这两个值与弹出/收起无关。修后 Dock 底 1020、任务栏 1020-1080，**零重叠** |
| **Dock 本体不置顶，最大化的窗口会盖住它** | 刻意的：需求是"Dock 只出现在桌面上，其他页面都在它之上"。所以 Dock 是普通窗口（`alwaysOnTop: false`，`layout_dock` / `move_to` 都带 `SWP_NOZORDER`），只有浮层（菜单 / 大文件夹 / 悬停标签）是 `TOPMOST`。实测：普通窗口盖住 Dock 中心点时最上层是那个窗口，关掉后又是 Dock。**若哪天要改**，正确做法是只对最大化窗口特判，而不是整体恢复置顶 |
| **外部程序能把 Dock 设成"总在最前"**（会浮在游戏上面） | `WS_EX_TOPMOST` 是**窗口自己的属性**，任何程序都能用 `SetWindowPos(HWND_TOPMOST)` 或直接改扩展样式给它设上（自动化脚本 / 录屏 / 游戏加速器 / "总在最前"小工具），而且**设上就不会自己掉** —— 表现为"我打开游戏，Dock 却盖在游戏上面"。2026-09 用户实测撞到过一次（来源是自检脚本为了点得到 Dock 临时置顶）。现在在 `dock_subclass_proc` 的 **`WM_WINDOWPOSCHANGING`（补 `SWP_NOZORDER`）/ `WM_STYLECHANGING`（抹掉该位）**里**于生效之前**拦掉：实测外部置顶后 40ms 内确认样式位从未变过（轮询版会暴露最多一秒，游戏里那一眼就够看见）；`reveal::clear_topmost_if_set` 降级为每 5 秒的兜底，正常永不触发。自检 `drive_layering_test` 6 项钉住它。附带影响：**Dock 从此不接受任何 Z 序变更** —— 想用"把它顶起来"的办法跑合成点击自检已失效，跑自检请 `Win+D` 让 Dock 真的露出来。见设计文档 §4.1.2 |
| ~~**debug 版启动会多带一个终端窗口**~~ | **已修**（2026-09）：现在**一直**是 GUI 子系统（`#![windows_subsystem = "windows"]`），双击启动不再有终端窗口。之所以以前要留着控制台，是因为 `println!` 在没有控制台时会**因为写失败而 panic**；现在输出统一走 `logging`（先落文件，控制台镜像写失败一律忽略），所以可以放心去掉。日志位置与崩溃取证见「环境变量 → 日志与崩溃取证」 |
| **做不到"把任务栏背景改透明"（需要注入 explorer）** | Win11 的任务栏背景是 Explorer 里的 **XAML 岛**画的，`SetWindowCompositionAttribute` 只能往上加色 → **只会更实**；DWM 背板那条路也排除了（`DWMWA_SYSTEMBACKDROP_TYPE` 设成 NONE 逐像素无变化）。TranslucentTB 在 Win11 是靠 `SetWindowsHookEx` + `InitializeXamlDiagnosticsEx` **注入 explorer** 改 XAML 画笔（GPL、10 文件 ~25KB C++），与本项目 R10「不注入任何进程」冲突。**要完全透明就用 TranslucentTB / Windhawk**；证据见 `docs/dock-technical-design.md` §4.1.3 |
| 无法操作以管理员身份运行的窗口 | UIPI；改为提供「以管理员身份运行」入口 |
| 自动隐藏用**单窗口**而非双窗口 | 把窗口整个移出屏幕即可不挡点击，不需要第二个触发条窗口 |
| 菜单是**独立窗口**（不是原生菜单） | 原生 `TrackPopupMenu` 外观完全无法定制，且自带模态消息循环卡住调用线程。代价：菜单窗口的**投影会被窗口边界裁掉**（亚克力按整个窗口矩形铺，留不出画阴影的边距），只用内描边区分边界 —— 见 `panel_window.rs` |
| 菜单的「点别处自动消失」靠**轮询**（16ms） | 菜单窗口是 `WS_EX_NOACTIVATE`（必须的：夺焦点会让用户正在打字的窗口失焦），因此收不到失活通知，没有消息可等。看门线程只在菜单打开期间存在 |
| 大文件夹**不支持嵌套**，也不能把图标从文件夹里**拖出来** | 嵌套会让"点开→再点开"变成一条没有尽头的路径；拖出来的落点语义（放回第几位？）没有好答案。要拿出来就用右键「解散文件夹」，或右键里面的图标「移出文件夹」 |
| 「移出文件夹」是**挪到顶层**，不是删掉 | 文案写的是"移出"，那就该是挪出去。想从 Dock 上删掉，移出来之后再右键「移出 Dock」——两步，但不含歧义 |
| 文件夹的「移出 Dock」会**连里面的应用一起删** | 里面的条目不在顶层列表里，只能跟着文件夹走。所以文案写明了项数（「移出 Dock（含里面 3 项）」），不做静默的数据损失 |
| 文件夹的图标预览只画**前 4 个** | 预览就那么大（2×2），多取是白花时间。里面超过 4 个时预览不代表全部 |
| Dock 上的分割线是**纯视觉**分组 | 它不参与「显示哪些应用」的任何逻辑，也不做智能排序（不会自动把同类应用分到一组）—— 位置完全由用户拖放决定 |
| 回收站的图标**不会**随空 / 满自动变化 | 图标带缓存（同一个 target 只取一次）。要跟随状态变化得定期查 `SHQueryRecycleBinW` 的 `dwNumItems` 并作废缓存，见 backlog |
| 手工按 stub 路径固定会「对不上」 | `System32\notepad.exe` 是应用执行别名，真实进程在 `WindowsApps` 下；**通过右键固定不会踩这个坑** |
| 少数商店应用快捷方式解析不出目标 | 有些 MSIX 别名的 `.lnk` 既没有文件路径、IDList 也拿不到可解析的名字；此时会**明确提示**改用托盘「添加应用…」 |
| 拖拽时被拖的图标超出面板会被窗口裁掉 | 窗口只有 72 逻辑像素高。macOS 能让图标浮在 Dock 外，我们做不到 —— 除非拖拽期间加高窗口，而那会让透明区域**吞掉桌面点击**（窗口透明 ≠ 点击穿透）。被裁掉这件事本身也成了「要删了」的视觉反馈 |

## 性能实测

| 指标 | 实测 |
|---|---|
| 主进程私有内存 | **7.7 MB** → ⚠️ **2026-09 复测是 18.3 MB（工作集 44.9 MB），这一格过期了** |
| 全部内存（主进程 + WebView2 子进程 ×7） | **242 MB** 私有（工作集 469 MB） → ⚠️ **2026-09 复测是 ~317 MB 私有（工作集 ~562 MB）、8 个 WebView2 进程 + 1 个 conhost，这一格也过期了** |
| 前端产物 | **71 kB gzip** |
| **空闲 CPU（自动隐藏开）** | **0.42 ~ 0.47% 单核** ← 见下方说明 |
| 空闲轮询节奏 | 1.50 秒/轮；点图标后 250ms 连打约 3.5 秒 |
| 前端轮询链路的净成本 | **0.15% 单核**（把轮询间隔改到 60 秒测出的差值） |
| `enumerate_apps()` 真实成本 | **2.6 ms/轮**（其中读 exe 版本资源占 1.9ms） |
| WebView2 子进程合计 | 0.26% 单核 |
| 单次窗口移动（含亚克力） | ~313 µs，占 60Hz 帧预算 6% |

> ⚠️ **内存那两格已过期，别直接引用**（2026-09 复测）：
> 主进程 `18.3 MB` 私有 / `44.9 MB` 工作集；子进程 **8 个** msedgewebview2 + 1 个 conhost，
> **稳定后**合计 **~287 MB 私有 / ~540 MB 工作集**（刚启动那 10 秒会到 ~330 MB，WebView2 还在预热；
> 最大单个渲染进程 ~139 MB）。旧的 7.7 / 242 / 「7 个进程」是**加大文件夹 + 悬停标签之前**测的。
> 数据来源：`docs/soak-log.txt`（`scripts/soak.ps1` 每 120 秒采一行）。

> ⚠️ **内存这一行又变了（2026-09）**：右键菜单改成**独立 WebView 窗口**之后，
> WebView2 子进程从 6 个变成 7 个，私有内存 191 MB → **242 MB**（+51 MB）。
> 主进程本身没变（8 → 7.7 MB）。代价换来的东西是右键菜单**即时出现** ——
> 窗口在启动时就建好并隐藏；改成"第一次右键再建"的话，那一次右键要等
> WebView2 初始化几百毫秒，看起来像卡了。取舍记在 `docs/backlog.md`。

> ⚠️ **这一栏的数字改过两次，看清版本再引用。**
>
> 1. 旧文档的「前端轮询 CPU **0.08%**」——**错的**，差一个数量级，那是 4 秒轮询时代
>    且没记录测法。
> 2. 随后实测 1.5 秒轮询下是 **2.34%** ——数字对，但**归因错了**：当时以为是轮询
>    造成的。
> 3. 最终定位（2026-09）：2.34% 里**轮询只占 0.15%**，其余是 `reveal.rs` 主循环
>    **每 16ms 无条件调一次 `SetWindowPos`**（62.5 Hz × ~313µs ≈ 1.96%）。
>    加了「矩形没变就跳过」的判断后降到 **0.42 ~ 0.47%**。
>
> 测法（含 A/B 归因方法）见 [`docs/backlog.md`](docs/backlog.md) 附录 A。
> ⚠️ **测 CPU 必须关掉 `DOCK_DEBUG`** —— 它自身的开销比被测对象还大。

> ⚠️ 设计文档原写「Tauri 常驻内存 40-80 MB」，**实测证明是错的**，
> 已在 `dock-technical-design.md` §2.1 显著标注修正。Tauri 的真实优势是**包体**与**Rust 互操作**，不是内存。

## 剩余工作（P1 收尾）

| 项 | 为什么没做完 |
|---|---|
| **人工确认**：UAC 弹窗、添加应用对话框 | 安全桌面弹窗无法脚本化 |
| **人工确认：从桌面真的拖一个图标到 Dock 上** | 拖放的**逻辑**已自检 7/7（解析/去重/排序/运行判定），但「OS 是否真的把拖放投递到我们的窗口」需要真实鼠标拖动 —— 合成一个 OLE 拖动循环不现实，且在桌面上模拟按下-拖动有误开文件的风险 |
| **8 小时长稳** | 需要**真实流逝的 8 小时**；会话内每轮只过去一两分钟。**采样脚本已就位**：`pwsh -File scripts/soak.ps1`（每 120 秒一行写进 `docs/soak-log.txt`，含 Dock 本体 + 7 个 WebView2 子进程的内存/句柄/线程/CPU）。旧记录（`docs/soak-log.txt` 第一段）只有 4 分钟，且以「进程已退出」结束 —— 那是一次**中断的**采样，不算数 |

> 「右键菜单视觉」已从人工项里去掉：现在有 `DOCK_MENUSHOT=<秒>` +
> 截屏核对（见上文环境变量表），菜单与 Dock 同框的截图已经比对过
> （浅色 Dock → 浅色菜单、深色文字；蓝色悬停高亮；分隔线位置正确）。

## 后续路线（P2+）

**要做的时候先看 [`docs/backlog.md`](docs/backlog.md)** —— 那里对「已经知道要改、
但现在不做」的项写了完整的前因后果、实测数字、代码位置和验收标准，
不用重新踩一遍坑。

- ~~**Dock 内部**的拖拽排序~~ —— **已有**：按住图标左右拖动改顺序，拖出 Dock 松手即移除
- ~~**右键菜单换成独立窗口**（还原 macOS 观感）~~ —— **已有**：`menu.rs` + `panel_window.rs`；
  没有投影（会被窗口边界裁掉），见「已知限制」
- ~~**大文件夹**（占一个图标位，点开在原地展开成图标网格）~~ —— **已有**：右键「新建文件夹」，把图标拖到文件夹上即可放入
- ~~文件夹命名（创建时固定叫「新建文件夹」）~~ —— **已有**（2026-09）：
  右键 →「新建文件夹…」弹一个**自绘输入框**让你填名字（默认「新建文件夹」，回车即用，Esc 取消）；
  文件夹右键有「重命名…」，默认值就是它现在的名字，改完立刻生效。
  为什么必须另开一个**能拿焦点**的窗口而不是画在 Dock 里：浮层窗口是 `WS_EX_NOACTIVATE`，
  **打不了字**（见 `prompt_window.rs` + `src/prompt/`）。
  ⚠️ 第一版是手搓的 Win32 输入框，用户说"太丑、内容还显示不全"（`CreateWindowExW` 的宽高
  含边框，我却按客户区排版，按钮被裁掉）—— 于是改成和浮层同一套玻璃观感的自绘窗口。
- ~~**分割线**（纯视觉分组）~~ —— **已有**：右键菜单「添加分割线」，位置由用户拖放决定
- ~~**系统位置**（此电脑 / 回收站）~~ —— **已有**：托盘 →「添加系统位置」，加进去落在最左侧
- Dock 本体上的「+」入口 —— **做过了又撤掉**（2026-09）：右端那个虚线「＋」按钮
  （点它开「添加应用…」）用户看了一眼说不要，于是删干净了。要加回来只需几处：
  `Dock.tsx` 里合成一个条目 + `App.tsx` 接 `pick_and_pin_app`。**别再加设置开关** ——
  上次就是"默认开 + 可关"，结果还是显得多余
- ~~设置界面~~ —— **已有**（托盘 →「设置…」）：基础信息 + 背景色动态调整。
  后续可加：图标尺寸/放大倍率的可视化调节、**Dock 内拖拽排序**（见 backlog BL-4）、图标列表的增删
- **[BL-1]** 用 `SetWinEventHook` 推送替换轮询 —— 详见 backlog。⚠️ 它的**CPU 理由
  不成立**（轮询只占空闲 CPU 的 0.15%，真凶是 `reveal.rs` 的 `SetWindowPos`，已修复），
  现在只剩「亚秒级延迟」和「为窗口预览 / 每窗口白点铺路」两条理由
- **[BL-2]** ~~先量清楚 `enumerate_apps()` 的 ~30ms 花在哪~~ —— **已完成**：
  真实成本 2.6ms（读 exe 版本资源占 1.9ms），且推翻了「轮询导致 2.1% CPU」这个前提
- **[BL-3]** 缓存 `file_description()`（低优先级，收益上限 ~0.1%）
- 最小化「吸入 Dock」缩放动画（`PrintWindow` 快照 + CSS 位移动画）
- 内存优化：WebView2 的 7 个子进程共 234 MB。其中最值得先看的是
  **菜单窗口要不要按需创建**（实测常驻它多花 ~51 MB；代价是第一次右键要等几百毫秒，
  见 backlog）
